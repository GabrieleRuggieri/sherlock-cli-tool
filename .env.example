# Pollinations (default, free cloud, no API key) does not require .env setup.

# Optional: only needed if you set provider to "groq" or "anthropic" in .codeopsrc
# Get keys at https://console.groq.com and https://console.anthropic.com
# GROQ_API_KEY=your_groq_key_here
# ANTHROPIC_API_KEY=your_anthropic_key_here
